# 工業5.0人機協作系統說明書
## Phase-1：折床工站 事件化證據鏈原型系統

> **文件基礎**：本說明書整合以下三份一手資料：
> 1. `論文.pdf`（论文全文：中小企業既有設備製造現場之工業5.0人機協作：事件化證據鏈方法與原型系統）
> 2. `硬體.pdf`（Phase-1 硬體组裝與佈線實務）
> 3. `研究說明書.md`（44篇文獻分析）
>
> **原則**：本文件僅整理上述來源可直接查核的事實，不添加推測或補足性內容。

---

## 目錄

1. [研究背景與問題定義](#1-研究背景與問題定義)
2. [核心術語定義表](#2-核心術語定義表phase-1)
3. [系統方法論：事件化證據鏈](#3-系統方法論事件化證據鏈)
4. [系統架構總覽](#4-系統架構總覽)
5. [硬體層設計](#5-硬體層設計)
6. [感知與同步層](#6-感知與同步層)
7. [幾何與追蹤層](#7-幾何與追蹤層)
8. [決策與邏輯層：PFID三源共識](#8-決策與邏輯層pfid三源共識)
9. [資料模型層：RBv3事件原子](#9-資料模型層rbv3事件原子)
10. [HMI與維運](#10-hmi與維運)
11. [Phase-1 驗證設計](#11-phase-1-驗證設計)
12. [量化指標定義](#12-量化指標定義)
13. [風險與限制](#13-風險與限制)
14. [文獻對照](#14-文獻對照)

---

## 1. 研究背景與問題定義

### 1.1 目標場域

| 項目 | 描述 |
|------|------|
| 場域類型 | 台灣中小企業板金加工廠（約30年歷史） |
| 設備環境 | Brownfield（新舊設備並存、介面不一、部分無標準通訊） |
| Phase-1 驗證工站 | 折床（Press Brake）工站 |
| 折床型號（參考） | Amada FBD8020NT |

### 1.2 核心問題陳述

論文將目標場域的製造現場問題歸納為以下三類可工程化處理的不確定性：

| 不確定性類別 | 具體現象 |
|------------|---------|
| **時間一致性不確定性** | 跨設備對時與觸發失鎖，導致跨源事件錯位 |
| **資料品質不確定性** | 遮擋、反光、感測波動造成判斷偏差，影響離線訓練評估結果 |
| **版本一致性不確定性** | 圖檔、規則、配方版本不一致，使流程宣告失真 |

### 1.3 製造現場的五個關鍵治理環節

論文原文歸納：

1. **設計與版本**：圖面/配方在換線與變更下容易產生版本不一致
2. **事件觸發**：現場需以機台作動事件界定「何時該看、該看哪一步」
3. **證據留存**：若缺乏可回放的證據，系統提示將難以取得現場信任
4. **低干擾回饋**：現場作業具節拍壓力，回饋需以不干擾生產為原則
5. **治理與改善**：將判斷結果與其依據留下，支援事後追溯與持續改善

### 1.4 研究目的（O1~O5）

| 目的代號 | 描述 |
|--------|------|
| O1 | 定義最小可追溯資料單位（RBv3事件原子） |
| O2 | 建立低侵入式事件時間軸（不改動安全回路） |
| O3 | 完成製程內提示與事後追溯（支援一鍵回放） |
| O4 | 建立可審計且可降級的治理機制（輸出理由碼） |
| O5 | 以階段性與對照實驗驗證可行性 |

### 1.5 研究問題（RQ）

| RQ代號 | 問題 |
|--------|------|
| RQ1 | 以事件窗切片與RBv3封裝後，是否能提升可追溯覆蓋率並縮短現場定位時間？ |
| RQ2 | 降級矩陣與理由碼是否能抑制誤放行/誤提示，且代價可被量化？ |
| RQ3 | 以file_pfid、recipe_hash與PFID共識治理版本一致性後，是否能提高版本錯載偵測率？ |

---

## 2. 核心術語定義表（Phase-1）

以下術語定義直接來自論文第2.1節：

| 名詞/欄位 | 代表型態 | 角色說明 |
|---------|---------|---------|
| `job_id` | string | 工單層觸發後生成之唯一識別鍵，作為本次加工的索引 |
| `host_ts` | int64 / ns | 主機時間戳（**唯一時間軸**），用於跨來源對齊與回放 |
| `event window` | [t−∆, t+∆] | 事件層最小封裝單位（以PLC edge定義），例如 ±300ms |
| `W` | SE(3) 參考系 | 統一空間口徑；離線地圖與線上觀測皆以W表達 |
| `RBv3` | record @100Hz | 事件原子（可回放/可稽核）資料格式，封裝證據索引、殘差、降級狀態與理由碼 |
| `file_pfid` | hash | 由DXF內容萃取之設計指紋；**不依賴檔名/資料夾命名** |
| `recipe_hash` | hash | 配方/容差/規則等設定指紋；用於版本一致性治理 |
| `plc.edge` | (ts, id) | PLC邊沿事件（HOLD_OK等），用於定義事件窗中心與步驟邊界 |
| `time.health` | struct | 同步健康度（例如 ptp_locked, vsync_ok），作為護欄與降級矩陣的輸入 |
| `reason_code` | enum/string | 以可回放方式描述拒答/降級原因（供UI與報表解釋） |
| `HOLD_OK` | PLC訊號 | 折床保壓到位訊號，**Phase-1 事件窗中心定義來源** |
| `W-Shift` | 空間微修操作 | 無停機低頻世界座標微修，由time.health護欄控制是否允許執行 |
| `map_id` | string | 工站切片地圖版本識別鍵 |
| `degrade_mode` | enum | 降級模式輸出（如 roi_only / advise_only） |

---

## 3. 系統方法論：事件化證據鏈

### 3.1 核心主張

論文提出「**事件化證據鏈（Event-based Evidence Chain）**」，其核心邏輯為：

> 以機台作動事件（PLC edge）作為**時間錨點**，  
> 整合設計意圖（DXF）、現場作動（PLC）、視覺證據（多視角相機），  
> 輸出**可回放、可稽核的事件單位（RBv3）**，  
> 失效時採**保守降級＋理由碼輸出**，不誤導現場。

### 3.2 與主流大廠路線的對比（論文Table 1）

| 面向 | 成熟 Greenfield（大廠路線） | 本研究：SME Brownfield（事件化證據鏈） |
|------|--------------------------|----------------------------------|
| 前提條件 | 介面/語意較一致、可投入較多工程維運 | 新舊設備並存、難以改動安全回路、換線頻繁 |
| 資料通道 | OPC UA/ISA-95等標準化整合 | 低侵入擷取（DI/影像/工單），以機台事件作時間錨點再封裝 |
| 狀態治理 | 設備生命週期標準化（如Sparkplug） | time.health＋版本鍵＋資料品質指標進入降級矩陣與理由碼輸出 |
| 決策型態 | 多服務分工，偏向「能跑」與「能維運」 | 強調「可回放、可稽核、可拒答」；不確定即保守 |
| 追溯與稽核 | 以批次/工單/報表追溯為主 | 每次判斷封為RBv3事件原子，支援 ±300ms 一鍵回放 |
| 評估指標 | OEE、良率、停機、產能等宏觀指標 | 延遲（P50/P95）、誤報/漏報、time-to-localize、可追溯覆蓋率、降級觸發與理由碼分佈 |

### 3.3 三層對照驗證設計

論文採三層對照設計確保結果可比較與可重現：

1. **現場基線對照**：以紙本/口頭SOP與事後抽查等既有作法作為基線
2. **A/B對照**：同一工站在「提示開/關」條件下比較延遲、誤報/漏報與定位時間
3. **消融對照（Ablation）**：移除關鍵治理機制（如多證據一致性或降級策略）觀察指標退化，驗證方法必要性

---

## 4. 系統架構總覽

### 4.1 雙層觸發設計

```
工單層觸發（掃碼/RFID）
  └─ 載入 DXF + 配方 + 版本鍵（file_pfid, recipe_hash）
  └─ 觸發CAD先驗導引的感知模組

事件層觸發（PLC edge：HOLD_OK 上升沿）
  └─ 定義事件窗中心 [t−300ms, t+300ms]
  └─ 擷取多視角影像
  └─ 執行一致性檢核、降級判斷
  └─ 封裝 RBv3 事件原子
```

### 4.2 兩條主線

| 主線 | 內容 |
|------|------|
| **離線建置（後台）** | 針對工站建立3DGS/NeRF切片地圖（map_id），並由DXF萃取幾何特徵、規則與版本鍵 |
| **線上運行（邊緣端）** | 以PLC edge做事件切片，於事件中心時間點抓取影像與切片地圖，完成對齊、一致性檢核與即時提示，並將所有證據封裝入RBv3 |

### 4.3 兩條時間路徑

| 狀態 | 路徑 |
|------|------|
| `time.health` 良好 | 嚴格同步路徑（外部觸發相機取像） |
| 對時或視覺品質不佳 | 依降級矩陣退回保守模式（roi_only 或僅提示不裁決） |

### 4.4 系統模組概覽（論文Table 4）

| 模組 | 輸入 | 輸出 |
|------|------|------|
| 資料擷取 | PLC/DI、相機影像、工單/DXF | 原始片段/時間戳 |
| 事件窗切片 | plc.edge, host_ts | event window [±300ms] |
| 一致性檢核 | 影像、地圖切片、CAD先驗 | 殘差指標（IoU/SSIM/reproj） |
| 降級/理由碼 | time.health、殘差、版本鍵 | verdict, reason_code |
| RBv3封裝 | 上述所有欄位 | RBv3 event atom |
| 回放/HMI | RBv3索引 | 提示/回放UI |

---

## 5. 硬體層設計

### 5.1 設計原則

> 「不改動安全回路、只讀監聽、可維修」（論文3.2節）

### 5.2 Phase-1 硬體清單 BOM（來自硬體.pdf）

| 項目 | 規格/型號 | 數量 | 備註 |
|------|---------|------|------|
| 邊緣端運算 | NVIDIA Jetson Orin Nano Super Developer Kit | 1 | 乙太網路接入工站獨立小LAN |
| 外部時戳節點 | BeagleBone Black Rev C（BBB） | 1 | GPIO邊沿擷取，輸出plc.edge |
| DI隔離/整形 | HY-M158（PC817 8路光耦）＋ SN74LVC14A（施密特整形） | 1組 | 禁止24V直接接Jetson/BBB；建議加PTC＋TVS（SMBJ33A） |
| 相機（主視角） | 奧比中光 Orbbec Gemini 335Lg × 2（GMSL2/FAKRA；需解串轉MIPI CSI-2） | 2 | 優先做硬體同步；固定Rig |
| 相機（補視角） | USB3 RGB-D（奧比中光 Orbbec Gemini 336） | 1 | 以事件窗切片為主，降低長時間錄影負擔 |
| 載板 | Seeed reComputer J401 Carrier Board | 1 | SOM、NVMe、DC IN等標準介面 |
| GMSL擴展板 | Robotics Board（MAX96712 GMSL Extension） | 1 | 提供GMSL2影像傳輸與多相機硬體同步 |
| 儲存 | NVMe SSD（M.2 2280 Key M） | 1 | 安裝JetPack作業系統與影像/點雲資料 |
| 工站交換器 | NETGEAR GS308（無管理8埠1GbE switch） | 1 | Jetson/BBB/後台主機直連；與外網隔離 |
| 後台主機 | 訓練/儲存/回放與報表 | 1 | Phase-1與邊緣端同交換器直連 |
| 電源供應 | 19V / 90W 變壓器 | 1 | 接J401載板DC IN |
| Mini-FAKRA線材 | GMSL2同軸線 | 若干 | 連接Gemini 335Lg ↔ GMSL擴展板 |
| PTC | 24V入口過流保護 | 1 | 串接於24V電源入口；配合TVS（SMBJ33A） |

### 5.3 DI訊號鏈路（硬體.pdf 接線圖）

```
折床PLC/端子台
  DI1..DI8（24V）
      │
      ▼  [PTC過流保護 + TVS（SMBJ33A）]
  PC817光耦（HY-M158 8路模組）
      │  電氣隔離 + 電平轉換
      ▼
  RC濾波（每路）
      │  去彈跳 / 低通
      ▼
  SN74LVC14A 施密特觸發器
      │  雜訊整形、乾淨3.3V邊沿
      ▼
  BeagleBone Black GPIO×8（3.3V）
      │  PRU邊沿時間戳
      ▼  [plc.edge：含channel/edge/timestamp]
  Ethernet（UDP/TCP 或 MQTT）
      │
      ▼
  Jetson Orin Nano（事件窗切片 + 封裝）
```

### 5.4 折床DI點位表（Amada FBD8020NT參考；最終以電氣迴路圖為準）

**組別一：基本操作與安全迴路**

| DI (X) | 功能名稱 | 說明 |
|--------|---------|------|
| X00.0 | E-Stop Main Circuit | 機器主電源急停迴路（全機斷電） |
| X00.1 | Light Curtain Active | 安全光柵遮斷訊號（遮斷即停止下壓） |
| X00.2 | Foot Switch (Down) | 腳踏開關：下行訊號（下壓動作觸發） |
| X00.3 | Foot Switch (Up/Release) | 腳踏開關：回升/放開訊號 |
| X00.4 | Safety Guard Door | 側邊/後方安全門鎖 |
| X01.0 | Pump ON Feedback | 主油泵馬達啟動指示 |
| X01.1 | Oil Overheat Alarm | 液壓油溫過高警報 |

**組別二：模式與軸向狀態**

| DI (X) | 功能名稱 | 說明 |
|--------|---------|------|
| X03.0 | Mode Select Manual | 模式選擇：手動 |
| X03.1 | Mode Select Auto | 模式選擇：自動 |
| X04.0 | Return to Origin Button | 原點復歸按鈕 |
| X04.1 | Single Stroke Button | 單行程操作按鈕 |
| X04.2 | Continuous Stroke Button | 連續操作按鈕 |

**組別三：機械極限與原點感應器**

| DI (X) | 功能名稱 | 說明 |
|--------|---------|------|
| X10.0 | D-Axis Origin Sensor | D軸（油缸深度）原點感應 |
| X10.1 | X-Axis Origin Sensor | X軸（後擋規前後）原點感應 |
| X10.2 | R-Axis Origin Sensor | R軸（後擋規上下）原點感應 |
| X11.0 | X-Axis (+) Over Travel | X軸正極限開關 |
| X11.1 | X-Axis (-) Over Travel | X軸負極限開關 |

**組別四：輔助功能**

| DI (X) | 功能名稱 | 說明 |
|--------|---------|------|
| X20.0 | Air Pressure OK | 氣壓確認訊號 |
| X20.1 | Lube System OK | 自動潤滑系統狀態 |

### 5.5 各硬體元件接線摘要

| 元件 | 目的 | 接線規則 |
|------|------|---------|
| **PC817光耦（HY-M158）** | 電氣隔離＋電平轉換 | 輸入端接DI（24V）；輸出端以3.3V上拉送往BBB；每路建議加RC |
| **SN74LVC14A** | 雜訊整形 | 供電3.3V；輸入接光耦輸出（可先RC濾波）；輸出接BBB GPIO |
| **PTC＋TVS** | 過流/突波保護 | PTC串接24V電源入口；TVS箝位在33V（SMBJ33A）吸收尖峰 |
| **BBB GPIO** | 邊沿時間戳 | 僅接3.3V；禁止24V直接接入；以Ethernet送plc.edge給Jetson |
| **Orbbec 335Lg** | 多視角RGB-D取像 | GMSL2/FAKRA → 解串 → MIPI CSI-2 → Jetson；優先硬體同步 |
| **NETGEAR GS308** | 工站內網 | Cat5e/Cat6連接Jetson/BBB/後台；建議工站LAN與外網隔離 |

### 5.6 上線Runbook（自檢清單）

1. **VSYNC/觸發線測**：確認觸發波形正常、無明顯抖動或重複觸發
2. **USB3壓力測**：連續取像，監控掉幀率、USB重新枚舉與長時間穩定性
3. **24V接地與光耦隔離**：確認24V與3.3V端隔離、GND走線清楚；觀察是否毛刺誤觸發

**班前自檢**：
- `time.health`：確認 `ptp_locked`, `vsync_ok`, `resync_count`
- 換線載入：`job_id`, `file_pfid`, `recipe_hash`
- PFID-Lite自測：以短事件窗檢查pfid共識與reason_code
- AP/網路基線：延遲、丟包與關鍵節點可達性

---

## 6. 感知與同步層

### 6.1 時間軸設計

- **唯一時間軸**：`host_ts`（主機時間戳，int64 / ns）
- **事件窗定義**：以 `HOLD_OK` 上升沿為中心，固定事件窗 **±300ms**
- **事件結束/分界**：HOLD_OK 下降沿（或釋放相關訊號）

### 6.2 對時策略

| 狀態 | 策略 |
|------|------|
| 對時健康（time.health OK） | 外部觸發同步相機取像；以主機時間戳記錄 |
| 對時抖動或失鎖 | 停止跨源插值與精細對齊；退回以PLC事件窗為主的保守模式 |

### 6.3 time.health 指標構成

| 欄位 | 意義 |
|------|------|
| `ptp_locked` | PTP對時鎖定狀態 |
| `vsync_ok` | 相機VSYNC同步正常 |
| `resync_count` | 重同步計數（趨勢監控） |

---

## 7. 幾何與追蹤層

### 7.1 空間口徑

- **唯一空間口徑**：世界座標 W（SE(3)參考系）
- 建置期完成相機Rig校準並固化外參；**線上不更新相機間外參**

### 7.2 Map–Live一致性檢核

每個事件窗內執行：
1. 以切片地圖（3DGS/NeRF）渲染期望觀測
2. 與當下影像進行對齊
3. 輸出殘差指標作為護欄依據

| 殘差指標 | 意義 | 用途 |
|----------|------|------|
| 重投影誤差（reproj_RMS） | 幾何對齊品質 | 護欄判斷、W-Shift依據、降級觸發 |
| 外觀相似度（IoU/SSIM） | 外觀一致性與遮擋影響 | 護欄判斷、資料品質標記 |
| 同步健康度（time.health） | 時間一致性狀態 | 允許W-Shift與否、決策降級 |

### 7.3 W-Shift（無停機幾何微修）

- **觸發條件**：time.health良好 且 殘差可被小幅修正（護欄成立）
- **執行方式**：低頻小幅世界座標微修（∆W）
- **版本化**：微修量與護欄證據寫入RBv3的 `calib.w_offset` 與 `guards` 欄位
- **護欄不成立時**：禁止微修，輸出理由碼，退回保守模式

---

## 8. 決策與邏輯層：PFID三源共識

### 8.1 三源指紋定義

| 指紋 | 來源 | 反映內容 |
|------|------|---------|
| `file_pfid` | DXF特徵摘要 + 版本鍵 | 設計意圖與版本一致性 |
| `vision_pfid` | 事件窗內對齊殘差＋關鍵ROI證據 | 現場觀測與地圖/幾何先驗的一致性 |
| `proc_pfid` | PLC事件型態＋時序片段（HOLD_OK邊沿型態） | 製程執行的可觀測狀態 |

### 8.2 二取三共識規則

$$v_{cons} = I(v_f + v_v + v_p \geq 2)$$

- $v_f, v_v, v_p \in \{0, 1\}$ 分別為設計/視覺/製程三源判定
- **任一來源缺失**：以 `reason_code` 記錄並觸發降級矩陣

### 8.3 降級矩陣邏輯（流程圖，來自硬體.pdf）

```
收到 plc.edge（HOLD_OK上升沿）
    │
    ▼
切事件窗 [t−∆, t+∆]
    │
    ▼
擷取影像 / 證據索引
    │
    ▼
計算殘差（IoU / SSIM / reproj）
    │
    ▼
time.health OK 且殘差 ≤ τ？
    ├─ No ──►  roi_only / advise_only（降級）
    │           輸出 reason_code
    └─ Yes ──► nominal（正常判斷）
                │
                ▼
            產生 verdict
                │
                ▼
        寫入 RBv3 event atom
        （可回放 / 可稽核）
```

### 8.4 LLM的角色限制

> 論文明確界定：「大型語言模型**僅**用於將理由碼轉譯為人員可讀說明，或生成報表摘要；  
> 其輸出**不參與線上裁決與放行判斷**。」

---

## 9. 資料模型層：RBv3事件原子

### 9.1 RBv3核心欄位（每個事件窗固定記錄）

| 欄位 | 類型 | 說明 |
|------|------|------|
| `host_ts` | int64/ns | 唯一時間軸 |
| `plc.edge` | struct | 含HOLD_OK邊沿事件 |
| `job_id` | string | 工單識別鍵 |
| `map_id` | string | 切片地圖版本 |
| `file_pfid` | hash | 設計指紋 |
| `recipe_hash` | hash | 配方/規則指紋 |
| `IoU` / `SSIM` | float | 一致性殘差 |
| `reproj_RMS` | float | 幾何對齊殘差 |
| `time.health` | struct | 同步健康度 |
| `verdict` | enum | 判定結果（放行/拒絕） |
| `reason_code` | enum/string | 拒答/降級理由 |
| `degrade_mode` | enum | 降級模式 |
| `calib.w_offset` | struct | W-Shift微修量（若觸發） |
| `guards` | struct | 護欄依據版本化記錄 |

### 9.2 端到端延遲定義

$$\Delta t_{e2e} = t_{done} - t_{edge}$$

- $t_{edge}$：plc.edge事件窗中心時間
- $t_{done}$：RBv3事件原子完成封裝並寫入完成之時間戳

---

## 10. HMI與維運

### 10.1 CAD→後台→Unity三段分工

| 工具 | 職責 |
|------|------|
| AutoCAD | 圖面產生與版本管理工具（僅為來源） |
| 後台/開發端（DXF解析器） | 萃取幾何特徵（外形輪廓、孔群、折線）→ 產生候選工序/成形步驟序列 → 輸出Unity可載入格式（JSON/二進位資產） |
| Unity端 | 將候選序列轉為可播放動畫時間軸；線上疊加ROI/幾何提示與理由碼；顯示操作者可理解的判斷依據 |

### 10.2 資料儲存策略

- **Unity**：僅顯示RBv3事件原子所索引之內容
- **大檔（影像/點雲/地圖切片）**：物件儲存管理
- **事件索引與判斷結果**：寫入RBv3
- **回放能力**：支援事後 ±300ms 一鍵回放

---

## 11. Phase-1 驗證設計

### 11.1 驗證場域說明

- **工站**：折床工站
- **驗證邊界**：「工站級閉環」——以單一工站作為最小可控單位
- **三類不確定性**各設計對應擾動與量化指標

### 11.2 Phase-1 驗證設計總覽（論文Table 6）

| 不確定性 | Phase-1 操作/擾動（例） | 量化指標（例） |
|---------|----------------------|--------------|
| 時間一致性 | 網路延遲/丟包、相機掉幀/失觸發、重同步 | event alignment error（ms/frames）、E2E latency（P50/P95）、resync_count、回放命中率 |
| 資料品質 | 遮擋/反光/視角缺失、畫面品質下降 | reproj_RMS、IoU/SSIM、拒答率、降級觸發率、誤報/漏報變化 |
| 版本一致性 | 換線錯載圖檔/配方、規則版本不一致 | 版本錯載偵測率、誤放行率、time-to-localize、可追溯覆蓋率 |

### 11.3 消融對照規劃

| 消融版本 | 移除機制 | 觀察指標 |
|---------|---------|---------|
| 消融A | 移除降級矩陣/reason_code護欄（僅保留基本規則） | 誤放行/誤提示是否上升 |
| 消融B | 移除PFID三源共識（改為單源或固定優先序） | 版本錯載偵測率與誤放行率變化 |
| 消融C | 停用W-Shift微修（固定W） | 長時間漂移下殘差、降級觸發率與覆蓋率變化 |

---

## 12. 量化指標定義

### 12.1 各評估指標（論文第5章）

**(1) 端到端延遲（E2E latency）**

$$\Delta t_{e2e} = t_{done} - t_{edge}$$

輸出：P50 / P95

**(2) 事件對齊誤差（event alignment error）**

對於第k路相機，其最近幀時間為 $t_{cam}^{(k)}$：

$$e_{align}^{(k)} = |t_{cam}^{(k)} - t_{edge}|$$

輸出：各路相機P95與最大值

**(3) 幾何對齊誤差（重投影誤差）**

$$e_{reproj} = \sqrt{\frac{1}{N}\sum_{i=1}^{N} \|x_i - \pi(KT_{CW}X_i)\|_2^2}$$

**(4) 區域一致性（IoU）**

$$IoU(A,B) = \frac{|A \cap B|}{|A \cup B|}$$

**(5) 可追溯覆蓋率（traceability coverage）**

令 $F_{req}$ 為必要欄位集合（job_id, host_ts, plc.edge, map_id, verdict, reason_code等）：

$$c_n = I(RBv3_n \text{ 包含 } F_{req})$$
$$Coverage = \frac{1}{N}\sum_{n=1}^{N} c_n$$

**(6) PFID三源共識（二取三）**

$$v_{cons} = I(v_f + v_v + v_p \geq 2)$$

### 12.2 評估指標定義表（論文Table 7）

| 指標 | 定義 | 資料來源 | 輸出 |
|------|------|---------|------|
| E2E latency | $t_{done} - t_{edge}$ | RBv3 host_ts / plc.edge | P50/P95 |
| 事件對齊誤差 | $\|t_{cam}^{(k)} - t_{edge}\|$ | 相機幀/DI時戳 | ms/frames |
| 重投影誤差 | $e_{reproj}$（RMS） | 幾何對齊/標定 | px |
| 區域一致性 | IoU(A,B) | 分割/ROI | IoU |
| 誤報/漏報 | TP/FP/FN（事件窗） | 人工標註/稽核 | rate |
| time-to-localize | $t_{resolve} - t_{alert}$ | HMI log/回放事件 | sec/min |
| traceability coverage | $\frac{1}{N}\sum c_n$ | RBv3 completeness | % |
| 降級觸發率 | $I(reason\_code \neq OK)$ | reason_code分佈 | % |

---

## 13. 風險與限制

### 13.1 論文明確列示的風險

| 風險 | 因應策略 |
|------|---------|
| vision_pfid相似工件可分性不足 → 判別力下降 | reason_code與降級矩陣限制風險外擴；事件化回放支援快速定位 |
| EMI/接地/線材造成DI誤觸發或通訊飽和 | 光耦隔離＋施密特整形＋PTC/TVS保護；接地點一致 |
| PTP長時間失鎖導致跨源對齊不可用 | 退回保守模式（roi_only）；reason_code記錄 |

### 13.2 適用邊界（論文第6章）

論文標記以下項目待補（表示Phase-1尚未完成現場驗證）：

- 工站類型限制（待補）
- 視角/遮擋條件限制（待補）
- 版本管理成熟度要求（待補）

### 13.3 論文尚未填入數據的待補項目

以下內容在論文中明確標記為「待補」，表示尚在驗證中：

- 實際驗收門檻數值（E2E latency P50/P95門檻值）
- 標註者角色與人數、標註規則
- A/B對照的交叉設計細節
- 消融版本清單與版本編號
- 相機型號/鏡頭/USB3同步線路完整規格
- 接地方案與誤觸發閾值

---

## 14. 文獻對照

論文第2章整理的文獻用途對應（論文Table 2）：

| 主題 | 代表性工作 | 本研究取用方式 |
|------|----------|---------------|
| 因子圖與增量平滑 | GTSAM / iSAM2 \[9,10\] | 作為多源不確定度融合與狀態更新的參考架構 |
| 事件窗與變點偵測 | BOCPD \[11\] | 以事件邊界切片建立可量測的對齊誤差 |
| 時序/步驟模型 | HSMM \[12\], TCN \[13\] | 以低延遲模型輸出phase/TTE等輔助訊號 |
| 拒答與不確定性治理 | Conformal / selective prediction \[14,15\] | 將「不確定就保守」制度化，支援降級策略 |
| 上游製程規劃（CAD/CAM/DFM） | 折彎/製造可行性規劃 \[16\] | 作為上游baseline：提供候選方案；本文聚焦線上事件化證據鏈與即時守門 |
| 資料譜系/溯源 | PROV-DM \[1-3\] | 以RBv3封裝支援審計與持續改善 |
| 工業互聯網架構 | IIRA \[4\] | 分層架構設計參考 |

---

*本說明書由論文全文（1008行）與硬體文件（393行）整合生成。*  
*最後更新：基於論文.pdf + 硬體.pdf + 研究說明書.md三份一手資料。*
